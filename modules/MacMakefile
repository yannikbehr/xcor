COMPL = gcc-3.3 		#on Max Os darwin
#COMPL = gcc			#on Linux (Debian Etch)
hfiles = \
	iniparser.h

ofiles = \
	iniparser.o

%.o:%.c 
	$(COMPL) -g -c  $<  

objects: $(ofiles) $(hfiles)

clean:
	rm *.o *.pyc
